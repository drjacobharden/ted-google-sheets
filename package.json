{
  "name": "ted-google-sheets",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "types": "bun x tsc --watch --noEmit",
    "dev": "bun run build & bun run types",
    "css": "cat src/main.css src/components/**/*.css src/elements/**/*.css src/screens/**/*.css > dist/bundle.css",
    "go": "bun run build & bun run css",
    "test": "bun test tests/app-state.test.ts tests/migration.test.ts"
  },
  "type": "module"
}
